# DynamisGames

Reference game applications built on the Dynamis engine.

## Build
cd DynamisGames && mvn clean install

## Adding a new game
1. Create a new directory under DynamisGames/
2. Add a pom.xml with parent dynamis-games
3. Add the module to the aggregator pom.xml
